<h1>Timeout Coding Challenge - Karl Charles</h1>

Step 1: in the terminal - `cd/api && npm install`

Step 2: `npm run start`. This will startup the json-server

Step 3: Open a new terminal window `cd ../social-event && npm install`

Step 4: `npm run serve` to run the app


<br><br>
<h2>To run test:</h2>

Step 1: (Terminal) In the social-event directory - `babel-upgrade --write --install`<br>
(This will get the babel-core 7.0^)

Step 2: `npm run test`